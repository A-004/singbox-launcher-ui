# Upcoming release — черновик

Сюда складываем пункты, которые войдут в следующий релиз. Перед релизом переносим в `X-Y-Z.md` и очищаем этот файл.

**Не добавлять** сюда мелкие правки **только UI** (порядок виджетов, выравнивание, стиль кнопок без смены действия и т.п.). Писать **новое поведение**: данные, форматы, сохранение, заметные для пользователя возможности.

## EN
### Highlights

### Technical / Internal
- [SPEC-064] Spec drafted: Fyne → Wails migration plan (HTML+CSS+JS UI)
- [prototype] Web dashboard HTTP server added (`webui/`) — localhost:9789, real-time proxy list + VPN toggle

## RU
### Основное
- Core Dashboard: адаптивная раскладка — при ширине окна < 451px элементы располагаются вертикально, при ≥ 451px — две колонки (слева трафик/информация, справа кнопка включения)
- Traffic widget: добавлено отображение геолокации (страна, город) — запрос к am.i.mullvad.net/json через VPN-туннель, обновление каждые 60 секунд
- Мониторинг трафика и пинг разделены на независимые таймеры: трафик обновляется каждые 2с, задержка (ping) каждые 3с, STUN+гео каждые 60с

### Техническое / Внутреннее
- [SPEC-064] Написан SPEC на миграцию UI с Fyne на Wails (HTML+CSS+JS)
- [prototype] Добавлен прототип веб-дашборда (`webui/`) — HTTP-сервер на localhost:9789, список прокси + переключение VPN в реальном времени
